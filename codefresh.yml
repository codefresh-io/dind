version: '1.0'

steps:
  build-step:
    type: build
    image-name: codefresh/dind

  push to registry:
    type: push
    candidate: ${{build-step}}
    tag: ${{CF_BRANCH}}

  push to dockerhub:
    type: push
    candidate: ${{build-step}}
    tag: ${{CF_BRANCH}}
    registry: dockerhub
